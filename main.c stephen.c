#include <stdio.h>
#include <stdlib.h>

int main()
{
    int pass;
    label:
          printf("change password\n");
          scanf("%d," $pass);
        {
              printf("welcome\n");

          }
          if(pass<2001)//you can change it;
            printf("wrong password\n re");
             go =label;
}
          (if(pass>2001)
          {
              printf("wrong password\n re");
              go to =lable;
}



()           (
             printf(" wrong password\n re"));
            go to lable;
          }
(
           return 0;

)
